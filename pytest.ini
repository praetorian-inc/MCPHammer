[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --cov=tools
    --cov=shared
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80
    -v
    --tb=short
asyncio_mode = auto
